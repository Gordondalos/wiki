
var string = "Какая то строка";

string.substring(10); // вернет строку в 10 символов сначала //

string.slice(-10); // вернет субстроку  только с конца

string.substring(14,4) // вернет подстроку с 14 символа 4 символа

s.IdexOf("точто ищем в этой строке")

s.split(".") // разобьет по строкам

s.toLowerCase();

s.toUpperCase();

s.CharAt(10); вернет 10-й символ

s[10]; // тоже вернет 10-й символ
